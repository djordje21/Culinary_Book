<h1 class="current-category"><%= params[:category] %></h1>

<% if @recipes.count == 0 %>
<h1> Sorry, there are no recipes curently in this category.. </h1>
<% else %>
	<% @recipes.each do |recipe| %>
 	<h2><%= link_to recipe.title, recipe_path(recipe) %></h2>

<% end %>
<% end %>


